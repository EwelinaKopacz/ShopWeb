import styled from 'styled-components';

const StyledH2 = styled.h2`
    display: flex;
    width: fit-content;
    height: 68px;
    align-items: center;
    font-size: var(--fs-cat-name);
    font-weight:var(--fw-regular) ;
    font-style: normal;
    line-height: 160%;
    color:var(--clr-text);
    margin:4rem 0 6rem 0;
    text-transform: capitalize;
`;

export default StyledH2;